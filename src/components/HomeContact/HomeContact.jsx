import React from "react";
// import data from "../../../data.json";

const HomeContact = () => {
  // console.log(data.data);
  return (
    <div>
      <div>
        {/* {data.data.map(({ item, name, image }) => (
          <div key={item}>
            <p>{item}</p>
            <p>{name}</p>
            <img src={require(`../../assets/images/${image}`)} alt="name" width={"200px"}/>
          </div>
        ))} */}
      </div>
    </div>
  );
};

export default HomeContact;
